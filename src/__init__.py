"""A harness for running long-lived Claude Code sessions against a target repository.

The package is import-only plumbing. The entry point is `harness.py` one
directory up, which is what a human or a cron job actually runs.
"""
